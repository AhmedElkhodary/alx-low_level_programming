#include "main.h"

/**
 * _isupper - check character is uppercase or not
 *
 * @c: character
 *
 * Return: 1(upercase)  0(otherwise)
 */

int main(int c)
{
	if (c >= 65 && c <= 90)
		return (1);

	return (0);
}
